import React from "react";
import "./Spinner.scss";

const Spinner = () => {
  return (
    <div className="spinnerOverlay">
      <div className="spinner" />
    </div>
  );
};

export default Spinner;
